#include <stdio.h>

int comparador(int n1, int n2, int n3, int n4) {
    int n5, n6, n7, n8;
    int retangulo1, retangulo2;

    retangulo1 = (n1, n2, n3, n4);
    retangulo2 = (n5, n6, n7, n8);
    
    if ()



}

int main (void) {
//declarando variáveis
    int a, b, c, d; //retangulo A
    int w, x, y, z; //retangulo B

    do {
        scanf("%d %d %d %d", &a, &b, &c, &d);
        printf("Retangulo 1: "a, b, c, d);
        if (a && b && c && d == 0){
            break;
        } else {
            scanf("%d %d %d %d", &w, &x, &y, &z);
            printf("Retangulo 2: " w, x, y, z);
        }
        

    } while();
    return 0;
}